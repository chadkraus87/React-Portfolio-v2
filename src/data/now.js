// ---------------------------------------------------------------------------
// NOW — an optional note in Chad's own words for /now. Everything else on that page
// comes from the site's data (public commits, project status, the daily demo check).
// Leave `note` empty to show only the data. `updated` is the day it was written.
//
//   export const now = { note: 'Rebuilding the HomeLab alerts pipeline.', updated: '2026-09-20' };
// ---------------------------------------------------------------------------
export const now = { note: '', updated: '' };
